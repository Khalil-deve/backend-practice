<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit User</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="mt-5 col-12 col-sm-10 offset-sm-1 col-md-8 offset-md-2 col-lg-6 offset-lg-3">
    <h2 class="text-center mb-4">Edit User Data</h2>

    <form action="/update/enroll/<%= user.enrollment_id %>" method="POST" class="p-4 bg-white shadow rounded">

      <div class="mb-3">
        <p>Student id: <strong><%= user.student_id %></strong></p>
        <p>Course id: <strong><%= user.course_id %></strong></p>
      </div>

      <div class="mb-3">
        <label for="grade" class="form-label">Email</label>
        <input type="text" name="grade" class="form-control" id="grade" value="<%= user.grade %>" style="width: 300px;" required>
      </div>

      <div class="text-center">
        <button type="submit" class="btn btn-success">Update</button>
        <a href="/show/data" class="btn btn-secondary">Cancel</a>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
