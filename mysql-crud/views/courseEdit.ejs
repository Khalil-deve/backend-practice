<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit User</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container mt-5">
    <h2 class="text-center mb-4">Edit User Data</h2>

    <form action="/update/course/<%= user.course_id %>" method="POST" class="p-4 bg-white shadow rounded">

      <div class="mb-3">
        <label for="course_name" class="form-label">Student Name</label>
        <input type="text" name="course_name" class="form-control" id="course_name" value="<%= user.course_name %>" required>
      </div>

      <div class="mb-3">
        <label for="instructor_name" class="form-label">Email</label>
        <input type="text" name="instructor_name" class="form-control" id="instructor_name" value="<%= user.instructor_name %>" required>
      </div>

      <div class="text-center">
        <button type="submit" class="btn btn-success">Update</button>
        <a href="/show/data" class="btn btn-secondary">Cancel</a>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
